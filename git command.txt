git status
git init			(to initialize/make the local folder into repo)
git add -A			(to allow git watch/track the files)
git commit -m "Any message"	(to 
git config --global user.name "ShaikhAtif"
git config --global user.email "k181122@nu.edu.pk"
